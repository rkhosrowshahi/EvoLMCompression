torch>=2.1
transformers>=4.40
pymoo>=0.6.1
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.7      # figures
# Needed twice over: gif encoding in evolmc/video.py, and transformers'
# image_utils does `PIL.Image.Resampling` unconditionally while loading ANY
# model. That enum arrived in 9.1.0, and transformers only declares Pillow
# under its `vision` extra -- so a core install can leave an ancient system
# Pillow in place and every from_pretrained() dies with
# "module 'PIL.Image' has no attribute 'Resampling'".
pillow>=10.0.1

# Real corpora (WikiText-2 / C4). Not needed for scripts/smoke_test.py.
datasets>=2.18

# Zero-shot task evaluation for the final Pareto front.
# lm-eval>=0.4.2

# Tests
pytest>=7.0

# Optional, not a Python package: ffmpeg enables mp4 encoding of the Pareto
# frames. Without it only the gif is written, via Pillow above.
#   macOS: brew install ffmpeg    Debian/Ubuntu: apt install ffmpeg
